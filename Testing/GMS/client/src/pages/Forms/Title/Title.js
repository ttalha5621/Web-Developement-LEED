import React from "react";

const Title = (props) => {
    return (
        <>
            <div class="card-header ">
                <h4 class="card-title">{props.Title}</h4>
            </div>
        </>
    );
}

export default Title;