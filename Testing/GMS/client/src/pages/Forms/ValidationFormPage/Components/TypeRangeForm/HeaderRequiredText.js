import React from "react";

const HeaderRequiredText = (props) => {
    return (
        <div className="card-header ">
            <h4 className="card-title">{props.Title}</h4>
        </div>
    );
}
export default HeaderRequiredText;