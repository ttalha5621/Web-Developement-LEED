import React from "react";

const SocialIcons = (props) => {
    return (
        <button href="#" className={props.Button}>
            <i className={props.Icon}></i>
        </button>
    )
}

export default SocialIcons;

